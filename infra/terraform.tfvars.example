# Copy to terraform.tfvars (gitignored) and fill in real values before
# running terraform plan/apply. Never commit real VPC IDs or CIDRs here.
vpc_id             = "vpc-xxxxxxxxxxxxxxxxx"
trusted_admin_cidr = "203.0.113.0/24" # replace with your actual office/VPN egress CIDR
